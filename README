This directory contains the source code used to run the
themes.rockbox.org website.

To create a local installation of the themes website, do the following:


1) Create a copy of an existing .ini file (e.g. called <yourname>.ini)
and edit appropriately.  The required variables are as follows:

DOCDIR - the htdocs directory to install the .php files to
DATADIR - directory containing the "themes.txt" database file for the site

